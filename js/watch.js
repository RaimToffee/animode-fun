import{api,esc,addHist,nav}from"./app.js";nav();let p=new URLSearchParams(location.search),ep=p.get("episodeId"),mv=p.get("movieId");try{let d=await api("/api/stream?"+(ep?"episodeId="+encodeURIComponent(ep):"movieId="+encodeURIComponent(mv))),s=d.stream||{},m=d.series||d.movie||{};document.querySelector("#title").textContent=m.title||"Watch";if(s.streamLink)document.querySelector("#player").innerHTML=`<iframe src="${esc(s.streamLink)}" allowfullscreen></iframe>`;else if(s.file)document.querySelector("#player").innerHTML=`<video controls playsinline src="${esc(s.file)}"></video>`;else document.querySelector("#player").innerHTML="<div class=error>No stream returned.</div>";if(ep)addHist({episodeId:ep,title:m.title||"Episode",image:m.poster||""})}catch{document.querySelector("#player").innerHTML="<div class=error>Unable to load stream.</div>"}
